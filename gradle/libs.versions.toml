[versions]
kotlin = "2.4.10"
kotlinx-coroutines = "1.11.0"
kotlinxDatetimeVersion = "0.7.1"
kotlinxIo = "0.9.1"
kotlinx-serialization = "1.11.0"
koog = "1.0.0"
mcp-kotlin = "0.8.3"
orgEclipseJgit = "7.7.0.202606012155-r"
deliverance = "0.0.11-SNAPSHOT"
skainet = "0.53.0"
skainet-transformers = "0.53.0"
slf4jSimple = "2.0.17"
ktor = "3.5.2"
shadow = "9.5.1"

[libraries]
kotlin-test = { module = "org.jetbrains.kotlin:kotlin-test", version.ref = "kotlin" }
kotlin-test-junit = { module = "org.jetbrains.kotlin:kotlin-test-junit", version.ref = "kotlin" }
kotlinx-datetime = { module = "org.jetbrains.kotlinx:kotlinx-datetime", version.ref = "kotlinxDatetimeVersion" }
kotlinx-coroutines-core = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-core", version.ref = "kotlinx-coroutines" }
kotlinx-coroutines-swing = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-swing", version.ref = "kotlinx-coroutines" }
kotlinx-coroutines-test = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-test", version.ref = "kotlinx-coroutines" }
kotlinx-io-core = { module = "org.jetbrains.kotlinx:kotlinx-io-core", version.ref = "kotlinxIo" }
mcp-kotlin-server = { group = "io.modelcontextprotocol", name = "kotlin-sdk-server", version.ref = "mcp-kotlin" }

eclipse-jgit = { module = "org.eclipse.jgit:org.eclipse.jgit", version.ref = "orgEclipseJgit" }
slf4j-simple = { module = "org.slf4j:slf4j-simple", version.ref = "slf4jSimple" }

ktor-server-core = { group = "io.ktor", name = "ktor-server-core", version.ref = "ktor" }
ktor-server-cio = { group = "io.ktor", name = "ktor-server-cio", version.ref = "ktor" }
ktor-server-sse = { group = "io.ktor", name = "ktor-server-sse", version.ref = "ktor" }
ktor-server-content-negotiation = { group = "io.ktor", name = "ktor-server-content-negotiation", version.ref = "ktor" }

ktor-client-core = { group = "io.ktor", name = "ktor-client-core", version.ref = "ktor" }
ktor-client-cio = { group = "io.ktor", name = "ktor-client-cio", version.ref = "ktor" }
ktor-client-content-negotiation = { group = "io.ktor", name = "ktor-client-content-negotiation", version.ref = "ktor" }
ktor-serialization-kotlinx-json = { group = "io.ktor", name = "ktor-serialization-kotlinx-json", version.ref = "ktor" }

kotlinx-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "kotlinx-serialization" }
koog-agents = { module = "ai.koog:koog-agents", version.ref = "koog" }

# Deliverance - Java-native LLM inference
deliverance-core = { module = "io.teknek.deliverance:core", version.ref = "deliverance" }
deliverance-safetensors = { module = "io.teknek.deliverance:safetensors", version.ref = "deliverance" }

# SKaiNET 0.53.0 — BOM-managed. The core BOM lives under the sk.ainet group;
# the library artifacts stay under sk.ainet.core (KMP: JVM consumers get the -jvm variant).
skainet-bom = { module = "sk.ainet:skainet-bom", version.ref = "skainet" }
skainet-lang-core = { module = "sk.ainet.core:skainet-lang-core" }
# KernelPacks.install() lives here (kernel SPI + view kernel packs).
skainet-backend-api = { module = "sk.ainet.core:skainet-backend-api" }
skainet-backend-cpu = { module = "sk.ainet.core:skainet-backend-cpu" }
# Native FFM kernel provider (bundled libskainet_kernels.so/.dylib/.dll): FP32 SGEMM plus the
# packed Q4_K/Q6_K/Q8_0 row-major matmuls that keep GGUF weights zero-copy. Auto-discovered via
# META-INF/services (KernelProvider + ViewKernelPack); silently falls back to Panama if the
# shared library cannot be extracted/loaded.
skainet-backend-native-cpu = { module = "sk.ainet.core:skainet-backend-native-cpu" }
skainet-io-core = { module = "sk.ainet.core:skainet-io-core" }
skainet-io-gguf = { module = "sk.ainet.core:skainet-io-gguf" }
# Typed suspend DataPipeline DSL + hf:// artifact resolver with local cache (JVM-only module).
skainet-data-source = { module = "sk.ainet.core:skainet-data-source" }

# SKaiNET-transformers 0.53.0 (sk.ainet.transformers group), BOM re-exports the core BOM.
skainet-tx-bom = { module = "sk.ainet.transformers:skainet-transformers-bom", version.ref = "skainet-transformers" }
# DecoderGgufWeightLoader, OptimizedLLMRuntime, generateUntilStop, PrefillStrategy, TokenizerFactory.
skainet-tx-core = { module = "sk.ainet.transformers:skainet-transformers-core" }
# AgentLoop, ToolRegistry, chat templates (Qwen3 / Llama 3), tool-call parsers.
skainet-tx-agent = { module = "sk.ainet.transformers:skainet-transformers-agent" }
skainet-tx-inference-llama = { module = "sk.ainet.transformers:skainet-transformers-inference-llama" }
skainet-tx-inference-qwen = { module = "sk.ainet.transformers:skainet-transformers-inference-qwen" }


[plugins]
kotlinMultiplatform = { id = "org.jetbrains.kotlin.multiplatform", version.ref = "kotlin" }
kotlinJvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
kotlinSerialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
shadow = { id = "com.gradleup.shadow", version.ref = "shadow" }
