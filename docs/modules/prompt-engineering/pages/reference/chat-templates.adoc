= Chat Templates Reference
:toc: left
:toclevels: 2
:icons: font

What each model family expects on the wire — system-message tool-schema
shape, response delimiter, response body, and which parser strategy
handles the response. Source files cited live under
`SKaiNET-transformers/llm-agent/src/commonMain/kotlin/sk/ainet/apps/kllama/chat/`
(SKaiNET-transformers 0.53.0).

== Which templates this app uses

Templates are chosen *per model family*, in code, with no CLI flag:

[cols="1,2,2"]
|===
|Stage |Template |Why

|Tool call (`qwen-commits`, Qwen3-0.6B)
|`QwenChatTemplate(enableThinking = false)`
|Qwen3 was trained on the ChatML + Hermes `<tool_call>` contract; disabling
thinking keeps a 0.6B model from spending its 192-token budget reasoning.

|Summary (`llama`, Llama-3.2-3B-Instruct)
|`Llama3ChatTemplate()` with an empty tool list
|Plain system + user turns. No tools are rendered, so the model never
sees Meta's tool-calling preamble.
|===

The rest of this page documents the formats the upstream library
supports, for readers who plug a different family into the pipeline
(xref:how-to/switch-chat-template.adoc[]).

== Quick comparison table

[cols="1,2,2,2,1"]
|===
|Family |Tool definition shape |Response delimiter |Response body example |Parser strategy

|Qwen 3.x (`QwenChatTemplate`) — *tool stage*
|`<tools>` block, one `{"type":"function","function":{…}}` per line
|`<tool_call>...</tool_call>`
|`<tool_call>{"name":"get_recent_commits","arguments":{"days":7}}</tool_call>`
|`HermesToolCallParserStrategy`

|Llama 3.2 (`Llama3ChatTemplate`, JSON format) — *summary stage, no tools*
|Bare JSON objects, `\n\n`-separated
|None — single-line JSON
|`{"name":"calculator","parameters":{"expression":"2+3"}}`
|`Llama31ToolCallParserStrategy`

|Llama 3.1 FUNCTION_TAG
|Bare JSON objects
|`<function=name>...</function>`
|`<function=calculator>{"expression":"2+3"}</function>`
|`Llama3FunctionTagParserStrategy`

|Gemma 2 / 3 (`GemmaChatTemplate`)
|`function_declarations` JSON array
|None — single-line JSON
|`{"functionCall":{"name":"calculator","args":{"expression":"2+3"}}}`
|`GemmaToolCallParserStrategy`

|Gemma 4 (`Gemma4ChatTemplate`)
|`<\|tool>` block, TS-style declarations
|`<\|tool_call>` block
|`call:calculator{expression:<\|"\|>2+3<\|"\|>}`
|`Gemma4ChatTemplate.parseToolCalls`

|ChatML / Hermes (`ChatMLTemplate`)
|JSON objects (one per line)
|`<tool_call>...</tool_call>`
|`<tool_call>{"name":"calculator","arguments":{"expression":"2+3"}}</tool_call>`
|`HermesToolCallParserStrategy`

|Apertus (`ApertusChatTemplate`)
|`<\|developer_start>` TS-style types
|`<\|tools_prefix>...<\|tools_suffix>`
|`<\|tools_prefix>[{"calculator":{"expression":"2+3"}}]<\|tools_suffix>`
|`ApertusToolCallParserStrategy`
|===

== Qwen 3.x (`QwenChatTemplate`) — the tool stage

Source: `QwenChatTemplate.kt`. Faithful to the `chat_template` shipped in
the official Qwen3 tokenizer configs (verified upstream against
`Qwen/Qwen3-0.6B`). This is what `QwenCommitsStage` renders; the exact
string of every run is logged as `[STAGE/QWEN RENDERED PROMPT]`.

Wire format:: ChatML turns: `<|im_start|>role\n…<|im_end|>\n`. Stop
tokens are `<|im_end|>` and `<|endoftext|>`; `LocalModel` passes the
first as the agent loop's `eosTokenId`.

System turn:: The caller's own system message (in this app
`QwenCommitsStage.DEFAULT_SYSTEM_PROMPT`) followed by the `# Tools`
section. Unlike generic ChatML there is *no* injected "You are Qwen…"
persona. Tools are listed one JSON object per line inside
`<tools></tools>`:

[source]
----
<|im_start|>system
You are a git assistant. You must call the get_recent_commits tool to look up commits; …

# Tools

You may call one or more functions to assist with the user query.

You are provided with function signatures within <tools></tools> XML tags:
<tools>
{"type":"function","function":{"name":"get_recent_commits","description":"…","parameters":{…}}}
</tools>

For each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:
<tool_call>
{"name": <function-name>, "arguments": <args-json-object>}
</tool_call><|im_end|>
----

Response:: The model answers with the same wrapper:

[source]
----
<tool_call>
{"name": "get_recent_commits", "arguments": {"days": 7}}
</tool_call>
----

Thinking:: Qwen3 emits `<think>…</think>` before its answer by default.
With `enableThinking = false` the generation prompt is pre-filled with an
empty think block — `<|im_start|>assistant\n<think>\n\n</think>\n\n` —
which is the official `enable_thinking=false` behaviour and what stops
the model from reasoning first. `parseThinkingBlocks` / `stripThinking`
still handle any block that slips through, so reasoning never lands in
the visible answer.

Tool result feedback:: A `TOOL` message renders as a *user* turn
containing `<tool_response>…</tool_response>`; consecutive results merge
into one user turn. The literal `tool` role never appears. In this app
the loop runs a single round (`maxToolRounds = 1`), so no
`<tool_response>` turn is ever rendered — the tool records its result
for the pipeline instead of telling the model.

Assistant replay:: Prior tool calls are replayed as `<tool_call>` blocks
rebuilt from the structured `ChatMessage.toolCalls`, not from raw text.
Again unused here because there is no second round.

== Llama 3.2 / 3.x (`Llama3ChatTemplate`) — the summary stage

Source: `Llama3ChatTemplate.kt`.

Without tools — which is how `LlamaGenerateStage` calls it
(`tools = emptyList()`) — the template emits the standard Llama 3 turn
structure:

[source]
----
<|begin_of_text|><|start_header_id|>system<|end_header_id|>

{system content}<|eot_id|><|start_header_id|>user<|end_header_id|>

{user content}<|eot_id|><|start_header_id|>assistant<|end_header_id|>

----

`<|begin_of_text|>` is a control token in the GGUF vocabulary, so the
stage encodes the rendered string as-is and does not add a BOS token
again. Generation runs `generateUntilStop` with the stop set
`{<|eot_id|>, <|end_of_text|>}`. The system and user text are logged as
`[STAGE/SYSTEM PROMPT]` / `[STAGE/USER PROMPT]`.

With tools (not used by this app) the template prepends Meta's
documented tool-calling system prompt to any user-supplied system
message. In the default `Llama3ToolFormat.JSON`:

[source]
----
You are a helpful assistant with tool calling capabilities.
When you receive a tool call response, use the output to format an answer
to the original user question.

You have access to the following functions:

{"name":"calculator","description":"…","parameters":{…}}

If you choose to call a function, your reply MUST be a single JSON object
on one line in the following format and nothing else:
{"name": <function-name>, "parameters": <arguments-object>}
Do not write the function definition. Do not include any prose. Do not use
variables.
----

Note `parameters`-not-`arguments` — Llama 3 uses `parameters` while Hermes
and ChatML use `arguments`. The parser accepts both as the args-object key
(`ToolCallParser.parseJsonToolCall`).

Tool result feedback:: A `TOOL` message becomes a `tool` role turn:
`<|start_header_id|>tool<|end_header_id|>\n\n<result text><|eot_id|>`.

Known reliability issue on small models:: Small Llama 3.2 variants
routinely ignore the "single JSON object on one line" instruction — see
xref:explanation/negative-sample-2026-05-05.adoc[the negative sample]
from the retired 1B single-model path. This is one reason the tool
stage moved to Qwen3 while Llama 3.2 3B only writes prose.

== Llama 3.1 FUNCTION_TAG

Selected via `Llama3ToolFormat.FUNCTION_TAG` in the
`Llama3ChatTemplate` constructor. The system message instructs:

[source]
----
If you choose to call a function, ONLY reply in the format below and
nothing else:
<function=function_name>{"arg_name": "arg_value"}</function>
Function calls MUST be on a single line. Required parameters MUST be
specified.
----

Used by Llama 3.1 instruct checkpoints; mostly superseded by the JSON
format in 3.2.

== Gemma 2 / 3 (`GemmaChatTemplate`)

Source: `GemmaChatTemplate.kt`.

Tool schema is wrapped as `function_declarations`:

[source,json]
----
[{
  "function_declarations": [
    {"name":"calculator","description":"…","parameters":{…}}
  ]
}]
----

The model returns `{"functionCall":{"name":"...", "args":{...}}}` (note
`args`, not `arguments` or `parameters`). Less battle-tested than Hermes.

== Gemma 4 (`Gemma4ChatTemplate`)

Source: `Gemma4ChatTemplate.kt`. Custom format with TypeScript-style
type declarations and a special `<|"|>` string delimiter (a single
sentencepiece token).

[source]
----
<|tool>declaration:calculator{description:<|"|>Evaluate…<|"|>,parameters:{…}}<tool|>
----

Response shape:

[source]
----
<|tool_call>call:calculator{expression:<|"|>2+3<|"|>}<tool_call|>
----

Custom brace-balanced parser inside `Gemma4ChatTemplate` (not JSON). If
you're not specifically targeting Gemma 4, prefer a different template —
the format is unique enough that mistakes silently produce unparseable
output.

== ChatML / Hermes (`ChatMLTemplate`)

Source: `ChatMLTemplate.kt`. ChatML is a generic format used by Mistral
instruct, OpenHermes, NousHermes, and many fine-tunes. The system
message instructs the model to wrap calls in `<tool_call>...</tool_call>`,
matching Qwen's response format. Same Hermes parser strategy. Differs
from `QwenChatTemplate` in the details that matter on small checkpoints
(tool-result role, one-tool-per-line listing, thinking prefill), so use
the Qwen template for Qwen checkpoints.

== Apertus (`ApertusChatTemplate`)

Source: `ApertusChatTemplate.kt`. TypeScript-style type declarations in
a `<|developer_start|>...<|developer_end|>` block:

[source]
----
// Evaluate simple arithmetic expressions
type calculator = (_: {
  expression?: string
}) => any;
----

Response is a JSON array of single-key objects in a tools-prefix/suffix
wrapper:

[source]
----
<|tools_prefix|>[{"calculator": {"expression":"2+3"}}]<|tools_suffix|>
----

Custom parser in `ApertusToolCallParserStrategy.kt`.

== When in doubt

* Running the default pipeline? Nothing to choose — Qwen3 renders through
  `QwenChatTemplate`, Llama 3.2 through `Llama3ChatTemplate`, picked by
  `ModelFamily`.
* Using the REST API backend? The remote server applies its own template;
  the summariser prompt is sent as plain system + user messages.
* Plugging a different local family into a custom stage? Match the
  template to the checkpoint's training format — Qwen/Hermes-trained
  models go through the best-tested parser.

== See also

* xref:reference/tool-call-parsers.adoc[Tool-call parsers] — what each
  parser strategy actually accepts.
* xref:how-to/switch-chat-template.adoc[How-to: switch chat template] —
  what changing a family involves.
